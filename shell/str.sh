name=''
sex='male'

if [ -z $name ]
then
	echo "name is null"
else
	echo "not null"
fi


if [ -z $age ]
then
	echo "age is null"
else
	echo "not null"
fi


if [ -z $sex ]
then
	echo "sex is null"
else
	echo "not null"
fi
